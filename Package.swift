// swift-tools-version:5.3

import PackageDescription

let package = Package(
    name: "EasyNotificationBadge",
    products: [
        .library(
            name: "EasyNotificationBadge",
            targets: ["EasyNotificationBadge"]),
    ],
    targets: [
        .target(
            name: "EasyNotificationBadge",
            dependencies: []),
    ]
)
